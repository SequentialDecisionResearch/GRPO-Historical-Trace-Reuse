# Data included in the GitHub-sized package

This directory contains the frozen split registry required for provenance.
Large raw/intermediate experiment artifacts are intentionally not committed to
ordinary Git history. In particular, `T03_overlap.csv` is omitted because of
its size. See `README_REPRODUCIBILITY.md` for the recorded expected hash and
archival strategy.
